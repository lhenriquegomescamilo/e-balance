ktor:
    application:
        modules:
            - com.ebalance.ApplicationKt.module
    deployment:
        port: 8080

database:
    url: "jdbc:postgresql://localhost:5432/ebalance"
    username: "ebalance"
    password: "ebalance"

# Path to the neural network model file produced by `./e-balance train --engine neural-network`
classifier:
    modelPath: "../nn-model.bin"

# SerpAPI key used to fetch live stock price history via Google Finance.
# Get your key at https://serpapi.com/
serpapi:
    apiKey: ""
