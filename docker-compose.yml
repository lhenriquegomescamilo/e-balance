services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ebalance
      POSTGRES_USER: ebalance
      POSTGRES_PASSWORD: ebalance
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
